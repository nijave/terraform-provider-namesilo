# The import ID is the domain name. Import seeds domain and id, then the
# refresh that follows reads the delegation from the API and stores the
# normalized nameservers set.
tofu import namesilo_nameservers.example example.com

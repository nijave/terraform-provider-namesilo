# The import ID is the domain name. Import seeds domain and id, then the
# refresh that follows reads the registrar lock from the API and stores the
# locked flag.
tofu import namesilo_domain_lock.example example.com

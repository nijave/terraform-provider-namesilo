---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "namesilo_contacts Data Source - namesilo"
subcategory: ""
description: |-
  Reads every contact profile in the account. Profiles are account-level records, so the data source has no input. The attributes that describe the contact person are Sensitive, mirroring the namesilo_contact resource: Terraform masks them in CLI output, but they still live in the state file, so treat that file as personal data.
  Use it to audit the account's profiles, to find the contact_id of a profile for the namesilo_domain_contacts resource, or to assert on profiles managed outside Terraform.
---

# namesilo_contacts (Data Source)

Reads every contact profile in the account. Profiles are account-level records, so the data source has no input. The attributes that describe the contact person are `Sensitive`, mirroring the `namesilo_contact` resource: Terraform masks them in CLI output, but they still live in the state file, so treat that file as personal data.

Use it to audit the account's profiles, to find the `contact_id` of a profile for the `namesilo_domain_contacts` resource, or to assert on profiles managed outside Terraform.

## Example Usage

```terraform
data "namesilo_contacts" "all" {}

output "default_contact_id" {
  value = [for contact in data.namesilo_contacts.all.contacts : contact.contact_id if contact.default_profile][0]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Read-Only

- `contacts` (Attributes Set) Every profile in the account. Attributes whose value the API reports as unset come back null. An account with no profiles yields an empty set. (see [below for nested schema](#nestedatt--contacts))
- `id` (String) Always `contacts`. It is inert and makes `tofu state show` readable.

<a id="nestedatt--contacts"></a>
### Nested Schema for `contacts`

Read-Only:

- `address` (String, Sensitive) Sensitive, like the same attribute of the `namesilo_contact` resource: Terraform masks it in CLI output, but the value still lives in the state file, so treat that file as personal data.
- `address2` (String, Sensitive) Sensitive, like the same attribute of the `namesilo_contact` resource: Terraform masks it in CLI output, but the value still lives in the state file, so treat that file as personal data.
- `ca_agreement_version` (String, Sensitive) Sensitive, like the same attribute of the `namesilo_contact` resource: Terraform masks it in CLI output, but the value still lives in the state file, so treat that file as personal data.
- `ca_language` (String, Sensitive) Sensitive, like the same attribute of the `namesilo_contact` resource: Terraform masks it in CLI output, but the value still lives in the state file, so treat that file as personal data.
- `ca_legal_form` (String, Sensitive) Sensitive, like the same attribute of the `namesilo_contact` resource: Terraform masks it in CLI output, but the value still lives in the state file, so treat that file as personal data.
- `ca_whois_display` (String, Sensitive) Sensitive, like the same attribute of the `namesilo_contact` resource: Terraform masks it in CLI output, but the value still lives in the state file, so treat that file as personal data.
- `city` (String, Sensitive) Sensitive, like the same attribute of the `namesilo_contact` resource: Terraform masks it in CLI output, but the value still lives in the state file, so treat that file as personal data.
- `company` (String, Sensitive) Sensitive, like the same attribute of the `namesilo_contact` resource: Terraform masks it in CLI output, but the value still lives in the state file, so treat that file as personal data.
- `contact_id` (String) The API's `contact_id`, an opaque account-scoped reference. Deliberately not `Sensitive`: it is needed as a plain reference, the way the resource's id is.
- `country` (String, Sensitive) Sensitive, like the same attribute of the `namesilo_contact` resource: Terraform masks it in CLI output, but the value still lives in the state file, so treat that file as personal data.
- `default_profile` (Boolean) Whether this is the account's default profile.
- `email` (String, Sensitive) Sensitive, like the same attribute of the `namesilo_contact` resource: Terraform masks it in CLI output, but the value still lives in the state file, so treat that file as personal data.
- `eu_citizenship_country` (String, Sensitive) Sensitive, like the same attribute of the `namesilo_contact` resource: Terraform masks it in CLI output, but the value still lives in the state file, so treat that file as personal data.
- `fax` (String, Sensitive) Sensitive, like the same attribute of the `namesilo_contact` resource: Terraform masks it in CLI output, but the value still lives in the state file, so treat that file as personal data.
- `first_name` (String, Sensitive) Sensitive, like the same attribute of the `namesilo_contact` resource: Terraform masks it in CLI output, but the value still lives in the state file, so treat that file as personal data.
- `last_name` (String, Sensitive) Sensitive, like the same attribute of the `namesilo_contact` resource: Terraform masks it in CLI output, but the value still lives in the state file, so treat that file as personal data.
- `nickname` (String, Sensitive) Sensitive, like the same attribute of the `namesilo_contact` resource: Terraform masks it in CLI output, but the value still lives in the state file, so treat that file as personal data.
- `phone` (String, Sensitive) Sensitive, like the same attribute of the `namesilo_contact` resource: Terraform masks it in CLI output, but the value still lives in the state file, so treat that file as personal data.
- `state` (String, Sensitive) Sensitive, like the same attribute of the `namesilo_contact` resource: Terraform masks it in CLI output, but the value still lives in the state file, so treat that file as personal data.
- `us_application_purpose` (String, Sensitive) Sensitive, like the same attribute of the `namesilo_contact` resource: Terraform masks it in CLI output, but the value still lives in the state file, so treat that file as personal data.
- `us_nexus_category` (String, Sensitive) Sensitive, like the same attribute of the `namesilo_contact` resource: Terraform masks it in CLI output, but the value still lives in the state file, so treat that file as personal data.
- `zip` (String, Sensitive) Sensitive, like the same attribute of the `namesilo_contact` resource: Terraform masks it in CLI output, but the value still lives in the state file, so treat that file as personal data.

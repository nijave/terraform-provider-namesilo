# terraform-provider-namesilo

A Terraform and OpenTofu provider for NameSilo registrar state — nameserver delegation, DS (DNSSEC) records, WHOIS privacy, the registrar lock, auto-renew, contact profiles, and domain contact associations — with a data source for the account's domain inventory. Work in progress; resources, data sources, and generated documentation arrive in later milestones.
